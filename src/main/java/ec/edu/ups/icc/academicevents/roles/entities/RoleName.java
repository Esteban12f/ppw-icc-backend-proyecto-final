package ec.edu.ups.icc.academicevents.roles.entities;

/**
 * Roles permitidos por la base de datos y utilizados por Spring Security.
 */
public enum RoleName {

    ADMIN,
    ORGANIZER,
    PARTICIPANT
}